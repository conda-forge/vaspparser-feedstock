schema_version: 1

context:
  name: vaspparser
  version: "0.0.12"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 2431ee1b89c5a9ba03e5795437eb9a569a114257dec5153b9f9a2461a07431b4

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
    - hatch-vcs
    - ase
    - defusedxml
    - numpy
    - pandas
    - scipy
  run:
    - python >=${{ python_min }}
    - ase >=3.23.0,<=3.29.0
    - defusedxml >=0.7.0,<=0.7.1
    - numpy >=1.26.0,<=2.5.3
    - pandas >=2.0.3,<=3.0.6
    - scipy >=1.11.1,<=1.18.1

tests:
  - python:
      imports:
        - vaspparser
      python_version:
        - ${{ python_min }}
        - "*"
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - pip check

about:
  license: BSD-3-Clause
  license_file: LICENSE
  summary: Parser for the Vienna Ab initio Simulation Package (VASP)
  homepage: https://github.com/pyiron/vaspparser

extra:
  recipe-maintainers:
    - jan-janssen
